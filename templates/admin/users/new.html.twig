{% extends 'admin/base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="container py-5">

        <a href="{{ path('admin_user_index') }}" class="btn btn-outline-secondary mb-4">← Back</a>

        <div class="card shadow-sm">
            <div class="card-body">
                <h3 class="mb-4">Create a user</h3>

                {{ include('admin/users/_form.html.twig', {button_label: 'Create'}) }}
            </div>
        </div>

    </div>
{% endblock %}
